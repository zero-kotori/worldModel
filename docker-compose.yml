services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: worldmodel
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - worldmodel-postgres:/var/lib/postgresql/data

volumes:
  worldmodel-postgres:
